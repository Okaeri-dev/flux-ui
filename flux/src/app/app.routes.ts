import { Routes } from '@angular/router';
import { VersionTxtComponent } from '@flux-components/version-txt/version-txt.component';

export const routes: Routes = [{ path: 'version.txt', component: VersionTxtComponent }];
